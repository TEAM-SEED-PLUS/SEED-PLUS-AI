services:
  ai:
    image: judemin/seed-plus-ai
    container_name: seed-plus-ai
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    init: true
